create table measurement (
	id int primary key,
	idx int,
    no2 double,
    o3 double,
    p double,
    pm10 double,
    pm25 double,
    so2 double,
    t double,
    w double,
    wg double,
    time date
);
